# CODE1 CODING HANDOFF — 2026-09-11

Status: WAITING_OPERATOR_SERVICE_ROLE_INPUT
Track: CODING
Branch: `coding/runtime-backend-staging`
Handoff source HEAD: `bda89fca3995ace0a5e40eba1e10745307b4d342`
Target: CODE1 Supabase STAGING `bsintmkyhptizrjoizfb` only
Live/Public Frontend/main/Production: UNCHANGED / NOT APPROVED
Planning delta seen: `20260910-002`

## Actual implementation state

The isolated backend migration is prepared through schema, security, source-normalization, import-preflight, retry-idempotency, exact private-source verification, and a Windows operator execution path. No source rows have yet been written to CODE1 STAGING because the connected Supabase SQL session is read-only and no connected tool exposes the service-role credential.

The operator no longer needs to place the service-role secret into Git, an `.env`, a browser bundle, command history, docs, or chat. `backend/staging/scripts/run-private-import.ps1` accepts it through a hidden PowerShell `SecureString` prompt, runs fail-closed preflight, requires exact target-ref confirmation before apply, and clears the process environment/zeroes the BSTR in `finally`.

`backend/staging/scripts/run-verified-private-import.mjs` pins the exact CODE1 STAGING ref plus the private snapshot SHA-256, byte size, and normalized source shape before any write, then verifies destination counts after apply.

## Verification result

Latest completed implementation verification before the handoff-document commit:

- GitHub Actions run: `34498679315` — SUCCESS
- Node/JavaScript syntax: PASS
- Windows PowerShell parser gate: PASS
- staging unit/contract tests: 41/41 PASS
- append-only retry idempotency test: PASS
- root regression: 35 PASS / 5 FAIL, exactly the five known pre-existing baseline failures
- root baseline comparison gate: PASS / zero new failures
- build: PASS

Known root baseline failures remain outside this backend workstream: deck-edit fixture, media-organizer filename assertion, two media-upload UX assertions, and `test/migration.test.mjs`.

## Infra changes in this work segment

GitHub isolated branch:

- added `backend/staging/scripts/run-private-import.ps1`;
- added `docs/coding/PRIVATE_STAGING_IMPORT_RUNBOOK_20260911.md`;
- extended isolated CI with a PowerShell parser gate;
- refreshed `CURRENT.md`, `BATON.md`, and the Supabase STAGING apply gate.

Google Drive:

- created My Drive root folder `[PRIVATE] CODE1 STAGING MIGRATION` (`1YMiqei4FbYe01V8RdN6x93Vjse4KPftw`);
- moved the verified private snapshot `CODE1_PRIVATE_SOURCE_SNAPSHOT_20260910.json` (`16iBk4-qDfIG1HlLlAzUsaUtOW4DQWJVm`) into that folder;
- snapshot SHA-256 remains `bde8f0671fd59bc125573129db7d4391bf0987380effcb50e91e43019eeb8f5b`, size `200458` bytes.

Supabase:

- no new DDL or DML in this work segment;
- source-import target remains empty;
- migrations `code1_0001` through `code1_0009` remain the applied CODE1 STAGING ledger.

Cloudflare / Apps Script / live:

- no change.

HOOOO / Production / Public Frontend / main:

- no change.

## Implemented facts that Planning may rely on as technical evidence

- canonical migration snapshot has farms 12, accounts 2, active questions 231, normalized submissions 2, answer revisions 5, commit sentinels 3, farm media 4, excluded DECK media 2, media events 5, access logs 29, login guard 3, question policies/history 0/0, housing-environment records 12 UNCONFIRMED, planning capabilities/brief versions/source artifacts 0/0/0;
- `01_농가_Master` rows 501–512 are a stale duplicate `GF-ORIGIN-01..12` block; migration uses rows 2–13 only and source normalization fails closed if duplicates are included;
- the source Sheet was not silently cleaned up;
- append-only legacy media/access events now retain `metadata.source_row`, preventing duplicate history rows during an explicitly controlled idempotent retry;
- existing SUPER_ADMIN/ADMIN roles do not implicitly receive Planning Delta capabilities;
- legacy housing `미확인`/blank never defaults to environment code 1 and migrates as NULL / UNCONFIRMED.

These are implementation/data-contract facts, not business-policy decisions.

## Planning-impacting technical constraints

- Actual STAGING import requires one operator-provided CODE1 `service_role` credential because the current connected SQL identity is read-only.
- The stale lower farm block is a source-data hygiene decision for Planning/operations to resolve separately; migration code only excludes it from this verified snapshot.
- Performance/index changes are intentionally deferred until imported workload measurements exist.
- R2/private-media migration remains gated behind successful DB source import and post-import verification.
- No code-derived value may be promoted into business policy without Planning decision.

## OPEN / WAITING

- `WAITING_OPERATOR_SERVICE_ROLE_INPUT`: actual CODE1 STAGING source import.
- `WAITING_IMPORT`: post-import exact-count/security/performance verification.
- `WAITING_DB_DATA_GATE`: R2/private-media migration.
- `OPEN_SEPARATE_DECISION`: cleanup of stale duplicate source rows 501–512.
- `OPEN_SEPARATE_HARDENING`: npm dependency findings, 3 high + 1 critical.

## Next atomic action

On the operator Windows machine:

1. update and check out `coding/runtime-backend-staging`;
2. download the private source JSON from `[PRIVATE] CODE1 STAGING MIGRATION` to a local path outside the repository;
3. run `backend/staging/scripts/run-private-import.ps1 -PreflightOnly` and enter the CODE1 STAGING service-role key only at the hidden prompt;
4. require `ok: true`, ref `bsintmkyhptizrjoizfb`, all-zero target, and `FIRST_IMPORT`;
5. run the wrapper again without `-PreflightOnly`, type the exact project ref when prompted, and let it execute import plus post-write count verification;
6. return only sanitized output, never the secret;
7. after import PASS, re-run exact DB counts, Security/Performance checks, and only then proceed to R2/private-media migration.

## Rollback boundary

Live remains unchanged and no dual-write exists. Until a separately approved cutover, rollback remains “do nothing”: current Apps Script/Sheet/Drive continues to be the live runtime and migration source.
