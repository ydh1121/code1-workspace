# CODE1 CODING HANDOFF — 2026-09-11

Status: STAGING_SOURCE_IMPORT_PASS / POST_IMPORT_DB_GATE_PASS / R2_PRIVATE_MEDIA_NEXT
Track: CODING
Branch: `coding/runtime-backend-staging`
Target: CODE1 Supabase STAGING `bsintmkyhptizrjoizfb` only
Live/Public Frontend/main/Production: UNCHANGED / NOT APPROVED
Planning delta seen: `20260910-002`

## Actual branch / implementation state

The isolated CODE1 runtime-backend migration has passed schema, security, source-normalization, retry-idempotency, private-source verification, operator preflight, actual source import, runner post-write verification, and independent Supabase count verification.

The operator used the Windows import path on branch `coding/runtime-backend-staging`. The latest code-bearing secret-input change is commit `fc947797f8ec34527c105ca6d35b437cc46fce96`; GitHub Actions run `34502664526` completed SUCCESS.

At explicit user request, `run-private-import.ps1` now uses normal visible terminal paste for the service-role value instead of masked/SecureString/clipboard-auto-read input. No actual secret value is stored in Git or this documentation. The same operator-input preference has been recorded in the CODE1 and cross-project Harness documents.

## Verified source import result

Preflight before write:

- project ref: `bsintmkyhptizrjoizfb`
- source SHA-256: `bde8f0671fd59bc125573129db7d4391bf0987380effcb50e91e43019eeb8f5b`
- source bytes: `200458`
- target counts: all zero
- mode: `FIRST_IMPORT`

Apply result:

- mode: `FIRST_IMPORT_APPLIED`
- workspace accounts: 2
- farms: 12
- question catalog: 231
- intake submissions: 2
- submission answer versions: 5
- media assets: 4
- media events: 5
- audit log: 29
- login guard: 3
- housing environment records: 12
- question policies/history: 0 / 0
- planning capabilities/brief versions/source artifacts: 0 / 0 / 0
- migration registry: 268

The import runner's post-write `actual` counts matched `expected` exactly.

Independent Supabase read after the operator import reconfirmed the same durable counts, including migration registry 268. No partial import mismatch was found.

## Security / performance verification

Post-import Security Advisor:

- WARN: 0
- only `rls_enabled_no_policy` INFO remains across the intentional server-only RLS boundary

Post-import Performance Advisor:

- unindexed foreign keys: 23 INFO
- unused indexes: 11 INFO

No index mutation was performed merely to silence INFO. Workload-based measurement remains required before tuning.

## Infra changes / non-changes

Supabase CODE1 STAGING now contains the verified source data. No Production project was created or connected.

The private source JSON remains outside Git in Drive folder `[PRIVATE] CODE1 STAGING MIGRATION`. The original Sheet was not silently cleaned.

Cloudflare live deployment, Apps Script live bridge, Public Frontend, formal Admin, main, HOOOO, INDX, and IndiaDesk were not changed by this import.

## Implemented technical facts relevant to Planning

- canonical migrated farms: 12;
- stale sparse duplicate farm block remains at source rows 501-512 and was excluded from migration, not deleted;
- DECK media rows 2 were excluded from farm runtime migration;
- legacy farm media count is 4 and remains Drive-backed pending R2;
- explicit blank answer revisions and append-only media/access history were preserved;
- retry identity uses `metadata.source_row` for append-only rows;
- housing environment unknown/blank remains NULL / UNCONFIRMED;
- Planning capabilities remain 0 and are not implied by existing admin roles.

These are implementation/data-contract facts, not business-policy decisions.

## OPEN / WAITING

- R2/private-media migration: NEXT, gated on actual CODE1 R2 bucket/binding verification
- same-action old/new p50/p95 performance comparison: WAITING Supabase-backed staging runtime path
- source rows 501-512 cleanup: OPEN_SEPARATE_DECISION
- npm dependency findings 3 high + 1 critical: OPEN_SEPARATE_HARDENING
- live cutover: NOT APPROVED
- Production: PROHIBITED in this workstream

## Next atomic action

Verify the existing CODE1 R2 bucket name and staging Worker binding/config without changing live deployment. Then implement the isolated STAGING private-media adapter/migration path, retain Drive legacy references as rollback evidence, exclude DECK media from farm runtime, and run staging-only verification before any cutover discussion.

## Rollback boundary

Live remains unchanged and no dual-write exists. Until separately approved cutover, existing Apps Script/Sheet/Drive remains the live runtime and rollback evidence.
