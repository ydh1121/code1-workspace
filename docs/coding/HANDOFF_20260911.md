# CODE1 CODING HANDOFF — 2026-09-11

Status: STAGING_SOURCE_IMPORT_PASS / POST_IMPORT_DB_GATE_PASS / R2_READ_ONLY_AUDIT_PASS / R2_RESOURCE_IDENTITY_BLOCKED
Track: CODING
Branch: `coding/runtime-backend-staging`
Target: CODE1 Supabase STAGING `bsintmkyhptizrjoizfb` only
Live/Public Frontend/main/Production: UNCHANGED / NOT APPROVED
Planning delta seen: `20260910-002`
Cross-track bus last seen: `MSG-20260911-0003`

## Actual branch / recovery state

The session recovered from the completed Supabase FIRST_IMPORT state and did not rerun import/preflight/schema work. At bootstrap, origin `coding/runtime-backend-staging` matched post-import HEAD `9066c104a1fbc34f2b597ba9f6df0781d00203ee`, whose CI run `34503734927` was SUCCESS.

The user's local PC working tree, local-only commits, and uncommitted files were not observable from this execution environment, so that portion remains `UNVERIFIED_LOCAL` rather than being guessed from origin.

## Verified source import result retained

Preflight before the already-completed write:

- project ref: `bsintmkyhptizrjoizfb`
- source SHA-256: `bde8f0671fd59bc125573129db7d4391bf0987380effcb50e91e43019eeb8f5b`
- source bytes: `200458`
- target counts: all zero
- mode: `FIRST_IMPORT`

Completed apply result:

- mode: `FIRST_IMPORT_APPLIED`
- workspace accounts: 2
- farms: 12
- question catalog: 231
- intake submissions: 2
- submission answer versions: 5
- media assets: 4
- media events: 5
- audit log: 29
- login guard: 3
- housing environment records: 12
- question policies/history: 0 / 0
- planning capabilities/brief versions/source artifacts: 0 / 0 / 0
- migration registry: 268

A fresh Supabase read during this R2 session reconfirmed those durable counts. No partial import mismatch was found and no DML/DDL was performed as part of that verification.

## Security / performance state retained

Post-import Security Advisor evidence remains:

- WARN: 0
- only `rls_enabled_no_policy` INFO remains across the intentional server-only RLS boundary

Post-import Performance Advisor evidence remains:

- unindexed foreign keys: 23 INFO
- unused indexes: 11 INFO

No index mutation was performed merely to silence INFO. Workload-based measurement is still required before tuning.

## R2/private-media READ-ONLY audit result

The isolated staging code already has a private R2 contract: `CODE1_MEDIA_BUCKET`, deterministic `private/farms/...` object keys, short-lived upload/read tokens, R2 PUT/GET handlers, object `HEAD` verification during finalization, farm authorization before read-token issuance, and soft-delete semantics.

The actual external CODE1 R2 resource could not be identified with sufficient account-level evidence:

- repository `wrangler.toml` has no `r2_buckets` binding or bucket name;
- Drive project documents describe the intended R2 architecture but do not establish an exact current CODE1-only bucket/binding identity;
- this session has no authenticated Cloudflare account inventory path for independently reading bucket list/configuration/bindings.

Result: `R2_RESOURCE_IDENTITY_BLOCKED`.

No R2 bucket was created, reused, bound, exposed, or written. No HOOOO/other-project resource was used.

## Legacy media source verification

The four imported farm-media metadata rows were inspected directly:

- `source_storage = GOOGLE_DRIVE_LEGACY`: 4/4
- `object_key = NULL`: 4/4
- current status `DELETED`: 4/4

Their four Drive source originals still exist as private owner-only files. Drive MIME and byte size match Supabase for 4/4. Actual source bytes were read and SHA-256 calculated for 4/4.

Detailed media IDs, Drive IDs, filenames, rights/consent fields, deletion timestamps, and checksums were deliberately kept out of Git. They are recorded in private Drive spreadsheet `CODE1_SOURCE_MEDIA_MANIFEST_20260911` under `[PRIVATE] CODE1 STAGING MIGRATION`, with R2 eligibility `HOLD_DELETED_RETENTION`.

The audit did not copy these deleted originals into R2 and did not delete the Drive rollback/retention source.

## New compatibility / retry evidence

A cutover blocker exists in the current implementation: the internal-web high-resolution client uses `mediaUpload.begin -> mediaUpload.chunk -> mediaUpload.finish`, but the isolated staging dispatcher currently implements begin/finish without `mediaUpload.chunk`.

Additional hardening items found from the current code contract:

- repeated `mediaUpload.begin` can create a new upload/media row for the same logical retry;
- repeated `mediaUpload.finish` can append duplicate finalization events in retry scenarios;
- the <=8 MiB compatibility upload can leave an orphan private object when R2 write succeeds and DB metadata commit fails;
- actual R2 PUT/HEAD/private GET/unauthorized denial and browser/runtime tests cannot be claimed until exact R2 resource identity is verified and bound to staging.

These are implementation constraints, not business-policy decisions.

## Isolated test increment

New mock-binding contract coverage was added for:

- R2 PUT missing-binding fail closed;
- invalid/expired upload authorization denial;
- exact declared size and MIME enforcement;
- exact authorized private object-key PUT;
- private GET missing-binding fail closed;
- invalid read-token denial before object access;
- authorized missing object 404;
- exact token object read and private response headers.

Branch CI run `34507001020` at commit `34d98fa836b90a541d42a2822719b3a22d18af4c` completed SUCCESS after these tests/documentation increments: staging unit/contract step PASS, root known-baseline gate PASS, build PASS.

This is not actual R2 integration evidence and is not `BROWSER_PASS`.

## Source boundary retained

- private source snapshot remains outside Git in `[PRIVATE] CODE1 STAGING MIGRATION`;
- stale `01_농가_Master` rows 501-512 remain unmodified and excluded from the verified snapshot;
- DECK media rows 2 remain excluded from farm runtime;
- deleted farm media remain Drive rollback/retention source pending separate retention/resource decisions;
- housing unknown/blank remains NULL / UNCONFIRMED;
- Planning capabilities remain 0 and are not implied by existing admin roles.

## Infra changes / non-changes

This session changed only isolated staging-branch tests/docs and a private Drive source-media manifest. It did not change Supabase data/schema, Cloudflare R2/bindings/deployment, Apps Script live bridge, Public Frontend, formal Admin, main, Production, HOOOO, INDX, or IndiaDesk.

## OPEN / WAITING

- exact CODE1 R2 bucket/account/binding identity: `BLOCKED / REQUIRED_BEFORE_R2_MUTATION`
- `mediaUpload.chunk` staging compatibility: OPEN
- begin/finish retry idempotency: OPEN
- small-upload object/metadata compensation or reconciliation: OPEN
- actual staging R2 PUT/HEAD/private GET/security integration: WAITING_R2_IDENTITY
- same-action old/new p50/p95 performance comparison: `NO_BASELINE` / WAITING runnable R2-backed staging path
- source rows 501-512 cleanup: OPEN_SEPARATE_DECISION
- npm dependency findings 3 high + 1 critical: OPEN_SEPARATE_HARDENING
- live cutover: NOT APPROVED
- Production: PROHIBITED in this workstream

## Next atomic action

Obtain current account-level Cloudflare evidence for the exact CODE1-only R2 bucket and its staging Pages/Worker binding. Verify bucket identity, account/resource identifier, private/public/custom URL state, CORS, lifecycle/retention configuration, existing-object state, binding name, and environment. Do not substitute another project's resource.

After the identity gate passes, close the `mediaUpload.chunk` compatibility and retry/compensation gaps, then run actual staging R2 PUT/HEAD/private GET/unauthorized-denial/DB-linkage tests. Keep deleted Drive source files as rollback/retention evidence until a separate policy permits physical purge.

## Rollback boundary

Live remains unchanged and no dual-write exists. Until separately approved cutover, existing Apps Script/Sheet/Drive remains the live runtime and rollback evidence.
