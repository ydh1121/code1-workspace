import test from 'node:test';import assert from 'node:assert/strict';import {normalizeSource,verifyNormalized} from '../src/source-normalizer.mjs';
const source={accounts:[{account_id:'OWNER',username:'owner',display_name:'Owner',role:'SUPER_ADMIN',status:'active',session_version:2}],farms:[{farm_id:'GF-1','내부 농가명':'농가1','상태':'검증중','Origin_12':'Y'}],questionCatalog:[{sort_order:1,section_code:'A',section_name:'기본',item_key:'A-01',item_label:'농장명',plain_question:'농장명?',input_type:'text',required_level:'REQUIRED',evidence_required:'FALSE',active:'TRUE'}],submissionQueue:[{submission_id:'SUB-1',submitted_at:'2026-09-01T00:00:00Z',farm_id:'GF-1',farm_name:'농가1',item_key:'A-01',input_type:'text',value_text:'"농가1"',status:'DRAFT',revision:1,request_id:'a'.repeat(32)},{submission_id:'SUB-1',submitted_at:'2026-09-01T00:00:00Z',farm_id:'GF-1',farm_name:'농가1',item_key:'__COMMIT__',input_type:'SYSTEM',value_text:'{}',status:'DRAFT',revision:1,request_id:'a'.repeat(32)}],mediaQueue:[{upload_id:'M-1',media_id:'M-1',submission_id:'SUB-1',farm_id:'GF-1',media_group:'PHOTO',file_name:'x.jpg',status:'REVIEW_REQUIRED'},{upload_id:'D-1',media_id:'D-1',submission_id:'DECK',media_group:'DECK',file_name:'d.jpg'}]};
test('sheet commit sentinel becomes submission revision, not answer',()=>{const n=normalizeSource(source),r=verifyNormalized(n);assert.equal(r.ok,true);assert.equal(n.submissions[0].current_revision,1);assert.equal(n.answers.length,1);assert.equal(n.answers[0].value_jsonb,'농가1');assert.equal(n.media.length,1);assert.equal(n.excluded.deckMedia.length,1);});
